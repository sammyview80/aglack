# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Agent/design scratch space — throwaway reference screenshots and notes that
# must never reach a public clone. Both patterns are intentional: `.tmp-ref/`
# names the directory we actually use, `.tmp*` catches future ad-hoc variants.
.tmp-ref/
.tmp*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
